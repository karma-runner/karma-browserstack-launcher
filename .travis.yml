sudo: false
language: node_js
node_js:
  - 8
  - 10

before_install:
  - npm config set loglevel warn

before_script:
  - npm install -g grunt-cli

script:
  - grunt

deploy:
  provider: npm
  email: "$NPM_USER_EMAIL"
  api-key: "$NPM_API_KEY"
  on:
    tags: true
